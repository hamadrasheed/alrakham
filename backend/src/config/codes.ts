import { ANY } from '../shared/common';

export const responses: ANY = {
    development: {
        en: {
            AUTH_FAILED: {
                message: 'Auth failed',
                status: 406
            },
        }
    },

};
